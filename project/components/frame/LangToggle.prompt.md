Language switch, sits in the frame's top-right beside CART.

```jsx
<LangToggle value={lang} onChange={setLang} />
```

Keep it to two languages. If a third is ever needed, stack them vertically rather than adding a menu.
