A single 1px rule, for cart line items and form groups.

```jsx
<Hairline />
<Hairline strong length="64px" />
```

Variants: `strong` (ink), `vertical`. If you find yourself using more than two per screen, use spacing
instead.
