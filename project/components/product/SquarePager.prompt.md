Carousel indicator under a product object.

```jsx
<SquarePager count={4} index={view} onChange={setView} />
```

Squares, 7px, --sp-3 apart. Do not round them and do not add arrows — the edges of the screen carry
BACK / NEXT instead.
