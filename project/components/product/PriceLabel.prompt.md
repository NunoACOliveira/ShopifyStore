Price block for the product page's right-hand column.

```jsx
<PriceLabel value="€ 65,00" />
```

Variants: `align`, custom `label` (e.g. `SHIPPING`). Sits between the title and the description with
generous space above and below.
