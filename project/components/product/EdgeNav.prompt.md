Edge-pinned previous/next words on the product screen.

```jsx
<EdgeNav onBack={prev} onNext={next} hideBack={isFirst} />
```

Hide an end rather than disabling it. Never replace the words with chevrons.
