The pre-entry screen. Fill the viewport with it and nothing else.

```jsx
<EnterSplash video="../../assets/splash.mp4" onEnter={() => setEntered(true)} />
```

The type is ink, not white — pick imagery light enough in the centre to carry it. Do not add a
protection gradient; choose a different frame instead.
