A row inside `CartPanel`.

```jsx
<CartLine image={p.image} name="STONE TEE" variant="brick / m" qty={1} price="€ 65,00" onRemove={rm} />
```

The only styling is the hairline underneath. Quantity is shown as `×1`, not a stepper.
